<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/ruby_detail"
    android:background="#314C5D">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitXY"
                android:src="@drawable/ruby_detail" />

        </FrameLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="-24dp"
            android:background="@drawable/rectangle_rounded_upside">

            <TextView
                android:id="@+id/ruby_title"
                style="@style/TextContent.Title"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="4dp"
                android:text="@string/ruby_title"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/ruby_subtitle"
                style="@style/TextContent.Subtitle"
                android:layout_below="@id/ruby_title"
                android:text="@string/ruby_subtitle"/>

            <TextView
                android:id="@+id/ruby_subtitle_detail"
                style="@style/TextContent.Detail"
                android:layout_below="@id/ruby_subtitle"
                android:text="@string/ruby_subtitle_detail"/>

            <TextView
                android:id="@+id/ruby_history"
                style="@style/TextContent.Subtitle"
                android:layout_below="@id/ruby_subtitle_detail"
                android:text="@string/ruby_history"/>

            <TextView
                android:id="@+id/ruby_history_detail"
                style="@style/TextContent.Detail"
                android:layout_below="@id/ruby_history"
                android:text="@string/ruby_history_detail"/>

        </RelativeLayout>

    </LinearLayout>

</ScrollView>