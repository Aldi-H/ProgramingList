<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/cs_detail"
    android:background="#314C5D">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitXY"
                android:src="@drawable/c__detail"/>

        </FrameLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="-32dp"
            android:background="@drawable/rectangle_rounded_upside">

            <TextView
                android:id="@+id/cs_title"
                style="@style/TextContent.Title"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="4dp"
                android:text="@string/cs_title"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/cs_subtitle"
                style="@style/TextContent.Subtitle"
                android:layout_below="@id/cs_title"
                android:text="@string/cs_subtitle"/>

            <TextView
                android:id="@+id/cs_subtitle_detail"
                style="@style/TextContent.Detail"
                android:layout_below="@id/cs_subtitle"
                android:text="@string/cs_subtitle_detail"/>

            <TextView
                android:id="@+id/cs_History"
                style="@style/TextContent.Subtitle"
                android:layout_below="@id/cs_subtitle_detail"
                android:text="@string/cs_history"/>

            <TextView
                android:id="@+id/cs_history_detail"
                style="@style/TextContent.Detail"
                android:layout_below="@id/cs_History"
                android:text="@string/cs_history_detail"/>

        </RelativeLayout>

    </LinearLayout>

</ScrollView>