<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/java_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#314C5D">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitXY"
                android:src="@drawable/java_detail" />

        </FrameLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="-54dp"
            android:background="@drawable/rectangle_rounded_upside">

            <TextView
                android:id="@+id/java_title"
                style="@style/TextContent.Title"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="4dp"
                android:text="@string/java"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/java_definition"
                style="@style/TextContent.Subtitle"
                android:layout_below="@id/java_title"
                android:text="@string/java_definition" />

            <TextView
                android:id="@+id/java_definition_detail"
                style="@style/TextContent.Detail"
                android:layout_below="@id/java_definition"
                android:text="@string/java_definition_detail"/>

        </RelativeLayout>

    </LinearLayout>

</ScrollView>